// enums

export enum Direction {
  HORIZONTAL = 'horizontal',
  VERTICAL = 'vertical',
}

export enum PosX {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
  TOP = 'top',
  BOTTOM = 'bottom',
  CENTR = 'center',
}

export enum PosY {
  TOP = 'top',
  BOTTOM = 'bottom',
  CENTER = 'center',
}
